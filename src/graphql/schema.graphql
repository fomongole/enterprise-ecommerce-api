# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input AddToCartInput {
  productId: String!
  quantity: Int!
  userId: String
}

type AuthResponse {
  accessToken: String!
  refreshToken: String!
  user: User!
}

type Cart {
  couponCode: String
  createdAt: DateTime!
  discountPercentage: Int!
  grandTotal: Float!
  id: ID!
  items: [CartItem!]!
  updatedAt: DateTime!
  user: User
  userId: String
}

type CartItem {
  cart: Cart!
  cartId: String!
  createdAt: DateTime!
  id: ID!
  product: Product!
  productId: String!
  quantity: Int!
  subTotal: Float!
  updatedAt: DateTime!
}

type Category {
  children: [Category!]
  createdAt: DateTime!
  description: String
  id: ID!
  name: String!
  parent: Category
  parentId: String
  slug: String!
  updatedAt: DateTime!
}

input CreateCategoryInput {
  description: String
  name: String!
  parentId: String
  slug: String!
}

input CreateOrderInput {
  shippingAddress: String!
}

input CreateProductInput {
  categoryId: String!
  description: String!
  images: [ProductImageInput!]
  name: String!
  price: Float!
  sku: String!
  slug: String!
  stock: Int!
}

input CreateReviewInput {
  comment: String
  productId: String!
  rating: Int!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  addToCart(input: AddToCartInput!): Cart!
  applyCoupon(code: String!): Cart!
  checkout(input: CreateOrderInput!): Order!
  createCategory(input: CreateCategoryInput!): Category!
  createCoupon(code: String!, discountPercentage: Int!): Promotion!
  createPaymentIntent(orderId: String!): PaymentIntentResponse!
  createProduct(input: CreateProductInput!): Product!
  createReview(input: CreateReviewInput!): Review!
  login(loginInput: LoginInput!): AuthResponse!
  mockPaymentSuccess(orderId: String!): Boolean!
  register(registerInput: RegisterInput!): AuthResponse!
}

type Order {
  createdAt: DateTime!
  id: ID!
  items: [OrderItem!]!
  shippingAddress: String!
  status: OrderStatus!
  stripePaymentId: String
  totalAmount: Float!
  updatedAt: DateTime!
  user: User!
  userId: String!
}

type OrderItem {
  createdAt: DateTime!
  id: ID!
  order: Order!
  orderId: String!
  price: Float!
  product: Product
  productId: String
  productName: String!
  quantity: Int!
  updatedAt: DateTime!
}

enum OrderStatus {
  CANCELLED
  DELIVERED
  PAID
  PENDING
  SHIPPED
}

type PaymentIntentResponse {
  clientSecret: String!
  id: String!
}

type Product {
  category: Category!
  categoryId: String!
  createdAt: DateTime!
  description: String!
  id: ID!
  images: [ProductImage!]
  isActive: Boolean!
  name: String!
  price: Float!
  reviews: [Review!]
  sku: String!
  slug: String!
  stock: Int!
  updatedAt: DateTime!
}

type ProductImage {
  altText: String
  createdAt: DateTime!
  id: ID!
  isPrimary: Boolean!
  product: Product!
  productId: String!
  updatedAt: DateTime!
  url: String!
}

input ProductImageInput {
  altText: String
  isPrimary: Boolean! = false
  url: String!
}

type Promotion {
  code: String!
  createdAt: DateTime!
  discountPercentage: Int!
  expiryDate: DateTime!
  id: ID!
  isActive: Boolean!
  updatedAt: DateTime!
}

type Query {
  categories: [Category!]!
  healthCheck: String!
  myCart: Cart!
  myOrders: [Order!]!
  product(id: String!): Product!
  productReviews(productId: String!): [Review!]!
  products: [Product!]!
}

input RegisterInput {
  email: String!
  firstName: String
  lastName: String
  password: String!
}

type Review {
  comment: String
  createdAt: DateTime!
  id: ID!
  product: Product!
  productId: String!
  rating: Int!
  updatedAt: DateTime!
  user: User!
  userId: String!
}

type User {
  cart: Cart
  createdAt: DateTime!
  email: String!
  firstName: String
  id: ID!
  isActive: Boolean!
  lastLogin: DateTime
  lastName: String
  orders: [Order!]
  role: UserRole!
  updatedAt: DateTime!
}

enum UserRole {
  ADMIN
  SUPPORT
  USER
}